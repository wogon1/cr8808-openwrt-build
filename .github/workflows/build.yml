name: Build CR8808 OpenWrt

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential clang flex bison g++ gawk gcc-multilib gettext \
            git libncurses5-dev libssl-dev python3 python3-distutils \
            rsync unzip zlib1g-dev file wget curl libelf-dev

      - name: Clone OpenWrt source for CR880x
        run: |
          git clone --depth=1 \
            -b ipq50xx-mainline-kernel-5.15-openwrt-23.05 \
            https://github.com/hzyitc/openwrt-redmi-ax3000.git openwrt

      - name: Add OpenClash package
        working-directory: openwrt
        run: |
          git clone --depth=1 https://github.com/vernesong/OpenClash.git /tmp/OpenClash
          cp -r /tmp/OpenClash/luci-app-openclash package/luci-app-openclash

      - name: Update feeds
        working-directory: openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Generate config
        working-directory: openwrt
        run: |
          cat > .config <<'EOF'
          CONFIG_TARGET_ipq50xx=y
          CONFIG_TARGET_ipq50xx_arm=y
          CONFIG_TARGET_ipq50xx_arm_DEVICE_redmi_ax3000=y

          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-ssl=y
          CONFIG_PACKAGE_luci-compat=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-ttyd=y

          CONFIG_PACKAGE_bash=y
          CONFIG_PACKAGE_ca-bundle=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_unzip=y
          CONFIG_PACKAGE_ip-full=y

          CONFIG_PACKAGE_dnsmasq=n
          CONFIG_PACKAGE_dnsmasq-full=y

          CONFIG_PACKAGE_kmod-tun=y
          CONFIG_PACKAGE_kmod-inet-diag=y
          CONFIG_PACKAGE_kmod-nft-tproxy=y
          CONFIG_PACKAGE_kmod-nft-socket=y
          CONFIG_PACKAGE_kmod-nft-nat=y

          CONFIG_PACKAGE_iptables-nft=y
          CONFIG_PACKAGE_ip6tables-nft=y
          CONFIG_PACKAGE_nftables-json=y

          CONFIG_PACKAGE_ruby=y
          CONFIG_PACKAGE_ruby-yaml=y
          EOF

          make defconfig

      - name: Show selected config
        working-directory: openwrt
        run: |
          grep -E "TARGET_|openclash|kmod-tun|kmod-nft-tproxy|kmod-nft-socket|dnsmasq-full" .config || true

      - name: Download packages
        working-directory: openwrt
        run: |
          make -j$(nproc) download

      - name: Build firmware
        working-directory: openwrt
        run: |
          make -j$(nproc) || make -j1 V=s

      - name: Collect firmware
        run: |
          mkdir -p output
          find openwrt/bin/targets -type f \( -name "*.ubi" -o -name "*.bin" -o -name "*.manifest" -o -name "*.buildinfo" -o -name "*.json" \) -exec cp -v {} output/ \;
          ls -lh output

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: cr8808-openwrt-openclash
          path: output/*
